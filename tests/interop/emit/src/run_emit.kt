// Generated by gen_types.cjs. DO NOT EDIT.
package alltypes
import specodec.*
import java.io.File

val testModels = listOf("Inner", "Coord", "IdVal", "Label", "Money", "Range32", "Addr", "Point3", "SingleString", "SingleBoolean", "SingleInt8", "SingleInt16", "SingleInt32", "SingleInt64", "SingleUint8", "SingleUint16", "SingleUint32", "SingleUint64", "SingleFloat32", "SingleFloat64", "SingleBytes", "PairString", "PairBoolean", "PairInt8", "PairInt16", "PairInt32", "PairInt64", "PairUint8", "PairUint16", "PairUint32", "PairUint64", "PairFloat32", "PairFloat64", "PairBytes", "DualStringInt32", "DualStringBoolean", "DualStringFloat64", "DualStringBytes", "DualInt32Boolean", "DualInt32Float64", "DualInt32Int64", "DualInt32Uint32", "DualInt64Uint64", "DualFloat32Float64", "DualFloat64Boolean", "DualFloat64Bytes", "DualUint32Uint64", "DualBooleanBytes", "DualInt8Uint8", "DualInt16Uint16", "DualStringInt64", "DualStringUint64", "DualInt32Bytes", "DualFloat64Int32", "DualBooleanInt32", "DualBytesInt64", "DualInt8Float32", "DualUint8Int16", "DualInt64Float64", "DualUint64String", "Triple01", "Triple02", "Triple03", "Triple04", "Triple05", "Triple06", "Triple07", "Triple08", "Triple09", "Triple10", "Triple11", "Triple12", "Triple13", "Triple14", "Triple15", "Five01", "Five02", "Five03", "Five04", "Five05", "Five06", "Five07", "Five08", "Five09", "Five10", "Ten01", "Ten02", "Ten03", "Ten04", "Ten05", "ArrString", "ArrInt32", "ArrBoolean", "ArrFloat64", "ArrBytes", "ArrInt64", "ArrUint64", "MultiArr1", "MultiArr2", "MultiArr3", "MultiArr4", "MultiArr5", "OptCombo1", "OptCombo2", "OptCombo3", "OptCombo4", "OptCombo5", "OptCombo6", "OptCombo7", "OptCombo8", "OptCombo9", "OptCombo10", "NestInner", "NestCoord", "NestIdVal", "NestLabel", "NestMoney", "NestRange32", "NestAddr", "NestPoint3", "OptNestInner", "OptNestCoord", "OptNestIdVal", "OptNestLabel", "OptNestMoney", "OptNestRange32", "OptNestAddr", "OptNestPoint3", "ModelArr1", "ModelArr2", "ModelArr3", "ModelArr4", "ModelArr5", "Mix01", "Mix02", "Mix03", "Mix04", "Mix05", "Mix06", "Mix07", "Mix08", "Mix09", "Mix10", "Mix11", "Mix12", "Mix13", "Mix14", "Mix15", "AllOpt1", "AllOpt2", "AllOpt3", "AllOpt4", "AllOpt5", "RecList", "RecTree", "RecChain", "RecWrap", "RecWide", "Wide20", "Wide25", "Wide30", "Wide35", "Wide40", "EdgeEmpty", "EdgeOneOpt", "EdgeBigNums", "EdgeZeroVals", "EdgeNullable", "EdgeNegZero", "EdgeNullByte", "EdgeBoundary", "EdgeStrLen", "EdgeBytesLen", "EdgeArrEmpty", "EdgeArrBoundary", "OptArr1", "OptArr2", "OptArr4", "OptArr5", "NestOpt1", "NestOpt2", "NestOpt3", "NestOpt4", "NestOpt5", "NestOptInner1", "NestOptInner2", "NestOptInner3", "DeepNest1", "DeepNest2", "DeepNest3", "DeepNest4", "DeepNest5", "DeepNest6", "DeepNest7", "TimestampEntry", "ConfigEntry")

val mpRoundtrip: Map<String, (ByteArray) -> ByteArray> = mapOf(
    "Inner" to { b: ByteArray ->
        val w = MsgPackWriter(); InnerCodec.encode(w, InnerCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Coord" to { b: ByteArray ->
        val w = MsgPackWriter(); CoordCodec.encode(w, CoordCodec.decode(MsgPackReader(b))); w.toBytes() },
    "IdVal" to { b: ByteArray ->
        val w = MsgPackWriter(); IdValCodec.encode(w, IdValCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Label" to { b: ByteArray ->
        val w = MsgPackWriter(); LabelCodec.encode(w, LabelCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Money" to { b: ByteArray ->
        val w = MsgPackWriter(); MoneyCodec.encode(w, MoneyCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Range32" to { b: ByteArray ->
        val w = MsgPackWriter(); Range32Codec.encode(w, Range32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Addr" to { b: ByteArray ->
        val w = MsgPackWriter(); AddrCodec.encode(w, AddrCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Point3" to { b: ByteArray ->
        val w = MsgPackWriter(); Point3Codec.encode(w, Point3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleString" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleStringCodec.encode(w, SingleStringCodec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleBoolean" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleBooleanCodec.encode(w, SingleBooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleInt8" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleInt8Codec.encode(w, SingleInt8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleInt16" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleInt16Codec.encode(w, SingleInt16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleInt32" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleInt32Codec.encode(w, SingleInt32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleInt64" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleInt64Codec.encode(w, SingleInt64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleUint8" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleUint8Codec.encode(w, SingleUint8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleUint16" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleUint16Codec.encode(w, SingleUint16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleUint32" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleUint32Codec.encode(w, SingleUint32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleUint64" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleUint64Codec.encode(w, SingleUint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleFloat32" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleFloat32Codec.encode(w, SingleFloat32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleFloat64" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleFloat64Codec.encode(w, SingleFloat64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "SingleBytes" to { b: ByteArray ->
        val w = MsgPackWriter(); SingleBytesCodec.encode(w, SingleBytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "PairString" to { b: ByteArray ->
        val w = MsgPackWriter(); PairStringCodec.encode(w, PairStringCodec.decode(MsgPackReader(b))); w.toBytes() },
    "PairBoolean" to { b: ByteArray ->
        val w = MsgPackWriter(); PairBooleanCodec.encode(w, PairBooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "PairInt8" to { b: ByteArray ->
        val w = MsgPackWriter(); PairInt8Codec.encode(w, PairInt8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairInt16" to { b: ByteArray ->
        val w = MsgPackWriter(); PairInt16Codec.encode(w, PairInt16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairInt32" to { b: ByteArray ->
        val w = MsgPackWriter(); PairInt32Codec.encode(w, PairInt32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairInt64" to { b: ByteArray ->
        val w = MsgPackWriter(); PairInt64Codec.encode(w, PairInt64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairUint8" to { b: ByteArray ->
        val w = MsgPackWriter(); PairUint8Codec.encode(w, PairUint8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairUint16" to { b: ByteArray ->
        val w = MsgPackWriter(); PairUint16Codec.encode(w, PairUint16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairUint32" to { b: ByteArray ->
        val w = MsgPackWriter(); PairUint32Codec.encode(w, PairUint32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairUint64" to { b: ByteArray ->
        val w = MsgPackWriter(); PairUint64Codec.encode(w, PairUint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairFloat32" to { b: ByteArray ->
        val w = MsgPackWriter(); PairFloat32Codec.encode(w, PairFloat32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairFloat64" to { b: ByteArray ->
        val w = MsgPackWriter(); PairFloat64Codec.encode(w, PairFloat64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "PairBytes" to { b: ByteArray ->
        val w = MsgPackWriter(); PairBytesCodec.encode(w, PairBytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringInt32" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringInt32Codec.encode(w, DualStringInt32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringBoolean" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringBooleanCodec.encode(w, DualStringBooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringFloat64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringFloat64Codec.encode(w, DualStringFloat64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringBytes" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringBytesCodec.encode(w, DualStringBytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt32Boolean" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt32BooleanCodec.encode(w, DualInt32BooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt32Float64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt32Float64Codec.encode(w, DualInt32Float64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt32Int64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt32Int64Codec.encode(w, DualInt32Int64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt32Uint32" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt32Uint32Codec.encode(w, DualInt32Uint32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt64Uint64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt64Uint64Codec.encode(w, DualInt64Uint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualFloat32Float64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualFloat32Float64Codec.encode(w, DualFloat32Float64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualFloat64Boolean" to { b: ByteArray ->
        val w = MsgPackWriter(); DualFloat64BooleanCodec.encode(w, DualFloat64BooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualFloat64Bytes" to { b: ByteArray ->
        val w = MsgPackWriter(); DualFloat64BytesCodec.encode(w, DualFloat64BytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualUint32Uint64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualUint32Uint64Codec.encode(w, DualUint32Uint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualBooleanBytes" to { b: ByteArray ->
        val w = MsgPackWriter(); DualBooleanBytesCodec.encode(w, DualBooleanBytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt8Uint8" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt8Uint8Codec.encode(w, DualInt8Uint8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt16Uint16" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt16Uint16Codec.encode(w, DualInt16Uint16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringInt64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringInt64Codec.encode(w, DualStringInt64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualStringUint64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualStringUint64Codec.encode(w, DualStringUint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt32Bytes" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt32BytesCodec.encode(w, DualInt32BytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "DualFloat64Int32" to { b: ByteArray ->
        val w = MsgPackWriter(); DualFloat64Int32Codec.encode(w, DualFloat64Int32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualBooleanInt32" to { b: ByteArray ->
        val w = MsgPackWriter(); DualBooleanInt32Codec.encode(w, DualBooleanInt32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualBytesInt64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualBytesInt64Codec.encode(w, DualBytesInt64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt8Float32" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt8Float32Codec.encode(w, DualInt8Float32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualUint8Int16" to { b: ByteArray ->
        val w = MsgPackWriter(); DualUint8Int16Codec.encode(w, DualUint8Int16Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualInt64Float64" to { b: ByteArray ->
        val w = MsgPackWriter(); DualInt64Float64Codec.encode(w, DualInt64Float64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DualUint64String" to { b: ByteArray ->
        val w = MsgPackWriter(); DualUint64StringCodec.encode(w, DualUint64StringCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple01" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple01Codec.encode(w, Triple01Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple02" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple02Codec.encode(w, Triple02Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple03" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple03Codec.encode(w, Triple03Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple04" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple04Codec.encode(w, Triple04Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple05" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple05Codec.encode(w, Triple05Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple06" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple06Codec.encode(w, Triple06Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple07" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple07Codec.encode(w, Triple07Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple08" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple08Codec.encode(w, Triple08Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple09" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple09Codec.encode(w, Triple09Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple10" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple10Codec.encode(w, Triple10Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple11" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple11Codec.encode(w, Triple11Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple12" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple12Codec.encode(w, Triple12Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple13" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple13Codec.encode(w, Triple13Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple14" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple14Codec.encode(w, Triple14Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Triple15" to { b: ByteArray ->
        val w = MsgPackWriter(); Triple15Codec.encode(w, Triple15Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five01" to { b: ByteArray ->
        val w = MsgPackWriter(); Five01Codec.encode(w, Five01Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five02" to { b: ByteArray ->
        val w = MsgPackWriter(); Five02Codec.encode(w, Five02Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five03" to { b: ByteArray ->
        val w = MsgPackWriter(); Five03Codec.encode(w, Five03Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five04" to { b: ByteArray ->
        val w = MsgPackWriter(); Five04Codec.encode(w, Five04Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five05" to { b: ByteArray ->
        val w = MsgPackWriter(); Five05Codec.encode(w, Five05Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five06" to { b: ByteArray ->
        val w = MsgPackWriter(); Five06Codec.encode(w, Five06Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five07" to { b: ByteArray ->
        val w = MsgPackWriter(); Five07Codec.encode(w, Five07Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five08" to { b: ByteArray ->
        val w = MsgPackWriter(); Five08Codec.encode(w, Five08Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five09" to { b: ByteArray ->
        val w = MsgPackWriter(); Five09Codec.encode(w, Five09Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Five10" to { b: ByteArray ->
        val w = MsgPackWriter(); Five10Codec.encode(w, Five10Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Ten01" to { b: ByteArray ->
        val w = MsgPackWriter(); Ten01Codec.encode(w, Ten01Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Ten02" to { b: ByteArray ->
        val w = MsgPackWriter(); Ten02Codec.encode(w, Ten02Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Ten03" to { b: ByteArray ->
        val w = MsgPackWriter(); Ten03Codec.encode(w, Ten03Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Ten04" to { b: ByteArray ->
        val w = MsgPackWriter(); Ten04Codec.encode(w, Ten04Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Ten05" to { b: ByteArray ->
        val w = MsgPackWriter(); Ten05Codec.encode(w, Ten05Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrString" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrStringCodec.encode(w, ArrStringCodec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrInt32" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrInt32Codec.encode(w, ArrInt32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrBoolean" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrBooleanCodec.encode(w, ArrBooleanCodec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrFloat64" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrFloat64Codec.encode(w, ArrFloat64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrBytes" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrBytesCodec.encode(w, ArrBytesCodec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrInt64" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrInt64Codec.encode(w, ArrInt64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ArrUint64" to { b: ByteArray ->
        val w = MsgPackWriter(); ArrUint64Codec.encode(w, ArrUint64Codec.decode(MsgPackReader(b))); w.toBytes() },
    "MultiArr1" to { b: ByteArray ->
        val w = MsgPackWriter(); MultiArr1Codec.encode(w, MultiArr1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "MultiArr2" to { b: ByteArray ->
        val w = MsgPackWriter(); MultiArr2Codec.encode(w, MultiArr2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "MultiArr3" to { b: ByteArray ->
        val w = MsgPackWriter(); MultiArr3Codec.encode(w, MultiArr3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "MultiArr4" to { b: ByteArray ->
        val w = MsgPackWriter(); MultiArr4Codec.encode(w, MultiArr4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "MultiArr5" to { b: ByteArray ->
        val w = MsgPackWriter(); MultiArr5Codec.encode(w, MultiArr5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo1" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo1Codec.encode(w, OptCombo1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo2" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo2Codec.encode(w, OptCombo2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo3" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo3Codec.encode(w, OptCombo3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo4" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo4Codec.encode(w, OptCombo4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo5" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo5Codec.encode(w, OptCombo5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo6" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo6Codec.encode(w, OptCombo6Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo7" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo7Codec.encode(w, OptCombo7Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo8" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo8Codec.encode(w, OptCombo8Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo9" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo9Codec.encode(w, OptCombo9Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptCombo10" to { b: ByteArray ->
        val w = MsgPackWriter(); OptCombo10Codec.encode(w, OptCombo10Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestInner" to { b: ByteArray ->
        val w = MsgPackWriter(); NestInnerCodec.encode(w, NestInnerCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestCoord" to { b: ByteArray ->
        val w = MsgPackWriter(); NestCoordCodec.encode(w, NestCoordCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestIdVal" to { b: ByteArray ->
        val w = MsgPackWriter(); NestIdValCodec.encode(w, NestIdValCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestLabel" to { b: ByteArray ->
        val w = MsgPackWriter(); NestLabelCodec.encode(w, NestLabelCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestMoney" to { b: ByteArray ->
        val w = MsgPackWriter(); NestMoneyCodec.encode(w, NestMoneyCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestRange32" to { b: ByteArray ->
        val w = MsgPackWriter(); NestRange32Codec.encode(w, NestRange32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestAddr" to { b: ByteArray ->
        val w = MsgPackWriter(); NestAddrCodec.encode(w, NestAddrCodec.decode(MsgPackReader(b))); w.toBytes() },
    "NestPoint3" to { b: ByteArray ->
        val w = MsgPackWriter(); NestPoint3Codec.encode(w, NestPoint3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestInner" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestInnerCodec.encode(w, OptNestInnerCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestCoord" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestCoordCodec.encode(w, OptNestCoordCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestIdVal" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestIdValCodec.encode(w, OptNestIdValCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestLabel" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestLabelCodec.encode(w, OptNestLabelCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestMoney" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestMoneyCodec.encode(w, OptNestMoneyCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestRange32" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestRange32Codec.encode(w, OptNestRange32Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestAddr" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestAddrCodec.encode(w, OptNestAddrCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptNestPoint3" to { b: ByteArray ->
        val w = MsgPackWriter(); OptNestPoint3Codec.encode(w, OptNestPoint3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ModelArr1" to { b: ByteArray ->
        val w = MsgPackWriter(); ModelArr1Codec.encode(w, ModelArr1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ModelArr2" to { b: ByteArray ->
        val w = MsgPackWriter(); ModelArr2Codec.encode(w, ModelArr2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ModelArr3" to { b: ByteArray ->
        val w = MsgPackWriter(); ModelArr3Codec.encode(w, ModelArr3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ModelArr4" to { b: ByteArray ->
        val w = MsgPackWriter(); ModelArr4Codec.encode(w, ModelArr4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "ModelArr5" to { b: ByteArray ->
        val w = MsgPackWriter(); ModelArr5Codec.encode(w, ModelArr5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix01" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix01Codec.encode(w, Mix01Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix02" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix02Codec.encode(w, Mix02Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix03" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix03Codec.encode(w, Mix03Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix04" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix04Codec.encode(w, Mix04Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix05" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix05Codec.encode(w, Mix05Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix06" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix06Codec.encode(w, Mix06Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix07" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix07Codec.encode(w, Mix07Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix08" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix08Codec.encode(w, Mix08Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix09" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix09Codec.encode(w, Mix09Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix10" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix10Codec.encode(w, Mix10Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix11" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix11Codec.encode(w, Mix11Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix12" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix12Codec.encode(w, Mix12Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix13" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix13Codec.encode(w, Mix13Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix14" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix14Codec.encode(w, Mix14Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Mix15" to { b: ByteArray ->
        val w = MsgPackWriter(); Mix15Codec.encode(w, Mix15Codec.decode(MsgPackReader(b))); w.toBytes() },
    "AllOpt1" to { b: ByteArray ->
        val w = MsgPackWriter(); AllOpt1Codec.encode(w, AllOpt1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "AllOpt2" to { b: ByteArray ->
        val w = MsgPackWriter(); AllOpt2Codec.encode(w, AllOpt2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "AllOpt3" to { b: ByteArray ->
        val w = MsgPackWriter(); AllOpt3Codec.encode(w, AllOpt3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "AllOpt4" to { b: ByteArray ->
        val w = MsgPackWriter(); AllOpt4Codec.encode(w, AllOpt4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "AllOpt5" to { b: ByteArray ->
        val w = MsgPackWriter(); AllOpt5Codec.encode(w, AllOpt5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "RecList" to { b: ByteArray ->
        val w = MsgPackWriter(); RecListCodec.encode(w, RecListCodec.decode(MsgPackReader(b))); w.toBytes() },
    "RecTree" to { b: ByteArray ->
        val w = MsgPackWriter(); RecTreeCodec.encode(w, RecTreeCodec.decode(MsgPackReader(b))); w.toBytes() },
    "RecChain" to { b: ByteArray ->
        val w = MsgPackWriter(); RecChainCodec.encode(w, RecChainCodec.decode(MsgPackReader(b))); w.toBytes() },
    "RecWrap" to { b: ByteArray ->
        val w = MsgPackWriter(); RecWrapCodec.encode(w, RecWrapCodec.decode(MsgPackReader(b))); w.toBytes() },
    "RecWide" to { b: ByteArray ->
        val w = MsgPackWriter(); RecWideCodec.encode(w, RecWideCodec.decode(MsgPackReader(b))); w.toBytes() },
    "Wide20" to { b: ByteArray ->
        val w = MsgPackWriter(); Wide20Codec.encode(w, Wide20Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Wide25" to { b: ByteArray ->
        val w = MsgPackWriter(); Wide25Codec.encode(w, Wide25Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Wide30" to { b: ByteArray ->
        val w = MsgPackWriter(); Wide30Codec.encode(w, Wide30Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Wide35" to { b: ByteArray ->
        val w = MsgPackWriter(); Wide35Codec.encode(w, Wide35Codec.decode(MsgPackReader(b))); w.toBytes() },
    "Wide40" to { b: ByteArray ->
        val w = MsgPackWriter(); Wide40Codec.encode(w, Wide40Codec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeEmpty" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeEmptyCodec.encode(w, EdgeEmptyCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeOneOpt" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeOneOptCodec.encode(w, EdgeOneOptCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeBigNums" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeBigNumsCodec.encode(w, EdgeBigNumsCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeZeroVals" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeZeroValsCodec.encode(w, EdgeZeroValsCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeNullable" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeNullableCodec.encode(w, EdgeNullableCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeNegZero" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeNegZeroCodec.encode(w, EdgeNegZeroCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeNullByte" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeNullByteCodec.encode(w, EdgeNullByteCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeBoundary" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeBoundaryCodec.encode(w, EdgeBoundaryCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeStrLen" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeStrLenCodec.encode(w, EdgeStrLenCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeBytesLen" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeBytesLenCodec.encode(w, EdgeBytesLenCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeArrEmpty" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeArrEmptyCodec.encode(w, EdgeArrEmptyCodec.decode(MsgPackReader(b))); w.toBytes() },
    "EdgeArrBoundary" to { b: ByteArray ->
        val w = MsgPackWriter(); EdgeArrBoundaryCodec.encode(w, EdgeArrBoundaryCodec.decode(MsgPackReader(b))); w.toBytes() },
    "OptArr1" to { b: ByteArray ->
        val w = MsgPackWriter(); OptArr1Codec.encode(w, OptArr1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptArr2" to { b: ByteArray ->
        val w = MsgPackWriter(); OptArr2Codec.encode(w, OptArr2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptArr4" to { b: ByteArray ->
        val w = MsgPackWriter(); OptArr4Codec.encode(w, OptArr4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "OptArr5" to { b: ByteArray ->
        val w = MsgPackWriter(); OptArr5Codec.encode(w, OptArr5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOpt1" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOpt1Codec.encode(w, NestOpt1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOpt2" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOpt2Codec.encode(w, NestOpt2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOpt3" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOpt3Codec.encode(w, NestOpt3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOpt4" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOpt4Codec.encode(w, NestOpt4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOpt5" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOpt5Codec.encode(w, NestOpt5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOptInner1" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOptInner1Codec.encode(w, NestOptInner1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOptInner2" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOptInner2Codec.encode(w, NestOptInner2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "NestOptInner3" to { b: ByteArray ->
        val w = MsgPackWriter(); NestOptInner3Codec.encode(w, NestOptInner3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest1" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest1Codec.encode(w, DeepNest1Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest2" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest2Codec.encode(w, DeepNest2Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest3" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest3Codec.encode(w, DeepNest3Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest4" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest4Codec.encode(w, DeepNest4Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest5" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest5Codec.encode(w, DeepNest5Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest6" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest6Codec.encode(w, DeepNest6Codec.decode(MsgPackReader(b))); w.toBytes() },
    "DeepNest7" to { b: ByteArray ->
        val w = MsgPackWriter(); DeepNest7Codec.encode(w, DeepNest7Codec.decode(MsgPackReader(b))); w.toBytes() },
    "TimestampEntry" to { b: ByteArray ->
        val w = MsgPackWriter(); TimestampEntryCodec.encode(w, TimestampEntryCodec.decode(MsgPackReader(b))); w.toBytes() },
    "ConfigEntry" to { b: ByteArray ->
        val w = MsgPackWriter(); ConfigEntryCodec.encode(w, ConfigEntryCodec.decode(MsgPackReader(b))); w.toBytes() },
)

val jsonRoundtrip: Map<String, (ByteArray) -> ByteArray> = mapOf(
    "Inner" to { b: ByteArray ->
        val w = JsonWriter(); InnerCodec.encode(w, InnerCodec.decode(JsonReader(b))); w.toBytes() },
    "Coord" to { b: ByteArray ->
        val w = JsonWriter(); CoordCodec.encode(w, CoordCodec.decode(JsonReader(b))); w.toBytes() },
    "IdVal" to { b: ByteArray ->
        val w = JsonWriter(); IdValCodec.encode(w, IdValCodec.decode(JsonReader(b))); w.toBytes() },
    "Label" to { b: ByteArray ->
        val w = JsonWriter(); LabelCodec.encode(w, LabelCodec.decode(JsonReader(b))); w.toBytes() },
    "Money" to { b: ByteArray ->
        val w = JsonWriter(); MoneyCodec.encode(w, MoneyCodec.decode(JsonReader(b))); w.toBytes() },
    "Range32" to { b: ByteArray ->
        val w = JsonWriter(); Range32Codec.encode(w, Range32Codec.decode(JsonReader(b))); w.toBytes() },
    "Addr" to { b: ByteArray ->
        val w = JsonWriter(); AddrCodec.encode(w, AddrCodec.decode(JsonReader(b))); w.toBytes() },
    "Point3" to { b: ByteArray ->
        val w = JsonWriter(); Point3Codec.encode(w, Point3Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleString" to { b: ByteArray ->
        val w = JsonWriter(); SingleStringCodec.encode(w, SingleStringCodec.decode(JsonReader(b))); w.toBytes() },
    "SingleBoolean" to { b: ByteArray ->
        val w = JsonWriter(); SingleBooleanCodec.encode(w, SingleBooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "SingleInt8" to { b: ByteArray ->
        val w = JsonWriter(); SingleInt8Codec.encode(w, SingleInt8Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleInt16" to { b: ByteArray ->
        val w = JsonWriter(); SingleInt16Codec.encode(w, SingleInt16Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleInt32" to { b: ByteArray ->
        val w = JsonWriter(); SingleInt32Codec.encode(w, SingleInt32Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleInt64" to { b: ByteArray ->
        val w = JsonWriter(); SingleInt64Codec.encode(w, SingleInt64Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleUint8" to { b: ByteArray ->
        val w = JsonWriter(); SingleUint8Codec.encode(w, SingleUint8Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleUint16" to { b: ByteArray ->
        val w = JsonWriter(); SingleUint16Codec.encode(w, SingleUint16Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleUint32" to { b: ByteArray ->
        val w = JsonWriter(); SingleUint32Codec.encode(w, SingleUint32Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleUint64" to { b: ByteArray ->
        val w = JsonWriter(); SingleUint64Codec.encode(w, SingleUint64Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleFloat32" to { b: ByteArray ->
        val w = JsonWriter(); SingleFloat32Codec.encode(w, SingleFloat32Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleFloat64" to { b: ByteArray ->
        val w = JsonWriter(); SingleFloat64Codec.encode(w, SingleFloat64Codec.decode(JsonReader(b))); w.toBytes() },
    "SingleBytes" to { b: ByteArray ->
        val w = JsonWriter(); SingleBytesCodec.encode(w, SingleBytesCodec.decode(JsonReader(b))); w.toBytes() },
    "PairString" to { b: ByteArray ->
        val w = JsonWriter(); PairStringCodec.encode(w, PairStringCodec.decode(JsonReader(b))); w.toBytes() },
    "PairBoolean" to { b: ByteArray ->
        val w = JsonWriter(); PairBooleanCodec.encode(w, PairBooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "PairInt8" to { b: ByteArray ->
        val w = JsonWriter(); PairInt8Codec.encode(w, PairInt8Codec.decode(JsonReader(b))); w.toBytes() },
    "PairInt16" to { b: ByteArray ->
        val w = JsonWriter(); PairInt16Codec.encode(w, PairInt16Codec.decode(JsonReader(b))); w.toBytes() },
    "PairInt32" to { b: ByteArray ->
        val w = JsonWriter(); PairInt32Codec.encode(w, PairInt32Codec.decode(JsonReader(b))); w.toBytes() },
    "PairInt64" to { b: ByteArray ->
        val w = JsonWriter(); PairInt64Codec.encode(w, PairInt64Codec.decode(JsonReader(b))); w.toBytes() },
    "PairUint8" to { b: ByteArray ->
        val w = JsonWriter(); PairUint8Codec.encode(w, PairUint8Codec.decode(JsonReader(b))); w.toBytes() },
    "PairUint16" to { b: ByteArray ->
        val w = JsonWriter(); PairUint16Codec.encode(w, PairUint16Codec.decode(JsonReader(b))); w.toBytes() },
    "PairUint32" to { b: ByteArray ->
        val w = JsonWriter(); PairUint32Codec.encode(w, PairUint32Codec.decode(JsonReader(b))); w.toBytes() },
    "PairUint64" to { b: ByteArray ->
        val w = JsonWriter(); PairUint64Codec.encode(w, PairUint64Codec.decode(JsonReader(b))); w.toBytes() },
    "PairFloat32" to { b: ByteArray ->
        val w = JsonWriter(); PairFloat32Codec.encode(w, PairFloat32Codec.decode(JsonReader(b))); w.toBytes() },
    "PairFloat64" to { b: ByteArray ->
        val w = JsonWriter(); PairFloat64Codec.encode(w, PairFloat64Codec.decode(JsonReader(b))); w.toBytes() },
    "PairBytes" to { b: ByteArray ->
        val w = JsonWriter(); PairBytesCodec.encode(w, PairBytesCodec.decode(JsonReader(b))); w.toBytes() },
    "DualStringInt32" to { b: ByteArray ->
        val w = JsonWriter(); DualStringInt32Codec.encode(w, DualStringInt32Codec.decode(JsonReader(b))); w.toBytes() },
    "DualStringBoolean" to { b: ByteArray ->
        val w = JsonWriter(); DualStringBooleanCodec.encode(w, DualStringBooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "DualStringFloat64" to { b: ByteArray ->
        val w = JsonWriter(); DualStringFloat64Codec.encode(w, DualStringFloat64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualStringBytes" to { b: ByteArray ->
        val w = JsonWriter(); DualStringBytesCodec.encode(w, DualStringBytesCodec.decode(JsonReader(b))); w.toBytes() },
    "DualInt32Boolean" to { b: ByteArray ->
        val w = JsonWriter(); DualInt32BooleanCodec.encode(w, DualInt32BooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "DualInt32Float64" to { b: ByteArray ->
        val w = JsonWriter(); DualInt32Float64Codec.encode(w, DualInt32Float64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt32Int64" to { b: ByteArray ->
        val w = JsonWriter(); DualInt32Int64Codec.encode(w, DualInt32Int64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt32Uint32" to { b: ByteArray ->
        val w = JsonWriter(); DualInt32Uint32Codec.encode(w, DualInt32Uint32Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt64Uint64" to { b: ByteArray ->
        val w = JsonWriter(); DualInt64Uint64Codec.encode(w, DualInt64Uint64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualFloat32Float64" to { b: ByteArray ->
        val w = JsonWriter(); DualFloat32Float64Codec.encode(w, DualFloat32Float64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualFloat64Boolean" to { b: ByteArray ->
        val w = JsonWriter(); DualFloat64BooleanCodec.encode(w, DualFloat64BooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "DualFloat64Bytes" to { b: ByteArray ->
        val w = JsonWriter(); DualFloat64BytesCodec.encode(w, DualFloat64BytesCodec.decode(JsonReader(b))); w.toBytes() },
    "DualUint32Uint64" to { b: ByteArray ->
        val w = JsonWriter(); DualUint32Uint64Codec.encode(w, DualUint32Uint64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualBooleanBytes" to { b: ByteArray ->
        val w = JsonWriter(); DualBooleanBytesCodec.encode(w, DualBooleanBytesCodec.decode(JsonReader(b))); w.toBytes() },
    "DualInt8Uint8" to { b: ByteArray ->
        val w = JsonWriter(); DualInt8Uint8Codec.encode(w, DualInt8Uint8Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt16Uint16" to { b: ByteArray ->
        val w = JsonWriter(); DualInt16Uint16Codec.encode(w, DualInt16Uint16Codec.decode(JsonReader(b))); w.toBytes() },
    "DualStringInt64" to { b: ByteArray ->
        val w = JsonWriter(); DualStringInt64Codec.encode(w, DualStringInt64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualStringUint64" to { b: ByteArray ->
        val w = JsonWriter(); DualStringUint64Codec.encode(w, DualStringUint64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt32Bytes" to { b: ByteArray ->
        val w = JsonWriter(); DualInt32BytesCodec.encode(w, DualInt32BytesCodec.decode(JsonReader(b))); w.toBytes() },
    "DualFloat64Int32" to { b: ByteArray ->
        val w = JsonWriter(); DualFloat64Int32Codec.encode(w, DualFloat64Int32Codec.decode(JsonReader(b))); w.toBytes() },
    "DualBooleanInt32" to { b: ByteArray ->
        val w = JsonWriter(); DualBooleanInt32Codec.encode(w, DualBooleanInt32Codec.decode(JsonReader(b))); w.toBytes() },
    "DualBytesInt64" to { b: ByteArray ->
        val w = JsonWriter(); DualBytesInt64Codec.encode(w, DualBytesInt64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt8Float32" to { b: ByteArray ->
        val w = JsonWriter(); DualInt8Float32Codec.encode(w, DualInt8Float32Codec.decode(JsonReader(b))); w.toBytes() },
    "DualUint8Int16" to { b: ByteArray ->
        val w = JsonWriter(); DualUint8Int16Codec.encode(w, DualUint8Int16Codec.decode(JsonReader(b))); w.toBytes() },
    "DualInt64Float64" to { b: ByteArray ->
        val w = JsonWriter(); DualInt64Float64Codec.encode(w, DualInt64Float64Codec.decode(JsonReader(b))); w.toBytes() },
    "DualUint64String" to { b: ByteArray ->
        val w = JsonWriter(); DualUint64StringCodec.encode(w, DualUint64StringCodec.decode(JsonReader(b))); w.toBytes() },
    "Triple01" to { b: ByteArray ->
        val w = JsonWriter(); Triple01Codec.encode(w, Triple01Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple02" to { b: ByteArray ->
        val w = JsonWriter(); Triple02Codec.encode(w, Triple02Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple03" to { b: ByteArray ->
        val w = JsonWriter(); Triple03Codec.encode(w, Triple03Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple04" to { b: ByteArray ->
        val w = JsonWriter(); Triple04Codec.encode(w, Triple04Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple05" to { b: ByteArray ->
        val w = JsonWriter(); Triple05Codec.encode(w, Triple05Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple06" to { b: ByteArray ->
        val w = JsonWriter(); Triple06Codec.encode(w, Triple06Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple07" to { b: ByteArray ->
        val w = JsonWriter(); Triple07Codec.encode(w, Triple07Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple08" to { b: ByteArray ->
        val w = JsonWriter(); Triple08Codec.encode(w, Triple08Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple09" to { b: ByteArray ->
        val w = JsonWriter(); Triple09Codec.encode(w, Triple09Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple10" to { b: ByteArray ->
        val w = JsonWriter(); Triple10Codec.encode(w, Triple10Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple11" to { b: ByteArray ->
        val w = JsonWriter(); Triple11Codec.encode(w, Triple11Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple12" to { b: ByteArray ->
        val w = JsonWriter(); Triple12Codec.encode(w, Triple12Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple13" to { b: ByteArray ->
        val w = JsonWriter(); Triple13Codec.encode(w, Triple13Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple14" to { b: ByteArray ->
        val w = JsonWriter(); Triple14Codec.encode(w, Triple14Codec.decode(JsonReader(b))); w.toBytes() },
    "Triple15" to { b: ByteArray ->
        val w = JsonWriter(); Triple15Codec.encode(w, Triple15Codec.decode(JsonReader(b))); w.toBytes() },
    "Five01" to { b: ByteArray ->
        val w = JsonWriter(); Five01Codec.encode(w, Five01Codec.decode(JsonReader(b))); w.toBytes() },
    "Five02" to { b: ByteArray ->
        val w = JsonWriter(); Five02Codec.encode(w, Five02Codec.decode(JsonReader(b))); w.toBytes() },
    "Five03" to { b: ByteArray ->
        val w = JsonWriter(); Five03Codec.encode(w, Five03Codec.decode(JsonReader(b))); w.toBytes() },
    "Five04" to { b: ByteArray ->
        val w = JsonWriter(); Five04Codec.encode(w, Five04Codec.decode(JsonReader(b))); w.toBytes() },
    "Five05" to { b: ByteArray ->
        val w = JsonWriter(); Five05Codec.encode(w, Five05Codec.decode(JsonReader(b))); w.toBytes() },
    "Five06" to { b: ByteArray ->
        val w = JsonWriter(); Five06Codec.encode(w, Five06Codec.decode(JsonReader(b))); w.toBytes() },
    "Five07" to { b: ByteArray ->
        val w = JsonWriter(); Five07Codec.encode(w, Five07Codec.decode(JsonReader(b))); w.toBytes() },
    "Five08" to { b: ByteArray ->
        val w = JsonWriter(); Five08Codec.encode(w, Five08Codec.decode(JsonReader(b))); w.toBytes() },
    "Five09" to { b: ByteArray ->
        val w = JsonWriter(); Five09Codec.encode(w, Five09Codec.decode(JsonReader(b))); w.toBytes() },
    "Five10" to { b: ByteArray ->
        val w = JsonWriter(); Five10Codec.encode(w, Five10Codec.decode(JsonReader(b))); w.toBytes() },
    "Ten01" to { b: ByteArray ->
        val w = JsonWriter(); Ten01Codec.encode(w, Ten01Codec.decode(JsonReader(b))); w.toBytes() },
    "Ten02" to { b: ByteArray ->
        val w = JsonWriter(); Ten02Codec.encode(w, Ten02Codec.decode(JsonReader(b))); w.toBytes() },
    "Ten03" to { b: ByteArray ->
        val w = JsonWriter(); Ten03Codec.encode(w, Ten03Codec.decode(JsonReader(b))); w.toBytes() },
    "Ten04" to { b: ByteArray ->
        val w = JsonWriter(); Ten04Codec.encode(w, Ten04Codec.decode(JsonReader(b))); w.toBytes() },
    "Ten05" to { b: ByteArray ->
        val w = JsonWriter(); Ten05Codec.encode(w, Ten05Codec.decode(JsonReader(b))); w.toBytes() },
    "ArrString" to { b: ByteArray ->
        val w = JsonWriter(); ArrStringCodec.encode(w, ArrStringCodec.decode(JsonReader(b))); w.toBytes() },
    "ArrInt32" to { b: ByteArray ->
        val w = JsonWriter(); ArrInt32Codec.encode(w, ArrInt32Codec.decode(JsonReader(b))); w.toBytes() },
    "ArrBoolean" to { b: ByteArray ->
        val w = JsonWriter(); ArrBooleanCodec.encode(w, ArrBooleanCodec.decode(JsonReader(b))); w.toBytes() },
    "ArrFloat64" to { b: ByteArray ->
        val w = JsonWriter(); ArrFloat64Codec.encode(w, ArrFloat64Codec.decode(JsonReader(b))); w.toBytes() },
    "ArrBytes" to { b: ByteArray ->
        val w = JsonWriter(); ArrBytesCodec.encode(w, ArrBytesCodec.decode(JsonReader(b))); w.toBytes() },
    "ArrInt64" to { b: ByteArray ->
        val w = JsonWriter(); ArrInt64Codec.encode(w, ArrInt64Codec.decode(JsonReader(b))); w.toBytes() },
    "ArrUint64" to { b: ByteArray ->
        val w = JsonWriter(); ArrUint64Codec.encode(w, ArrUint64Codec.decode(JsonReader(b))); w.toBytes() },
    "MultiArr1" to { b: ByteArray ->
        val w = JsonWriter(); MultiArr1Codec.encode(w, MultiArr1Codec.decode(JsonReader(b))); w.toBytes() },
    "MultiArr2" to { b: ByteArray ->
        val w = JsonWriter(); MultiArr2Codec.encode(w, MultiArr2Codec.decode(JsonReader(b))); w.toBytes() },
    "MultiArr3" to { b: ByteArray ->
        val w = JsonWriter(); MultiArr3Codec.encode(w, MultiArr3Codec.decode(JsonReader(b))); w.toBytes() },
    "MultiArr4" to { b: ByteArray ->
        val w = JsonWriter(); MultiArr4Codec.encode(w, MultiArr4Codec.decode(JsonReader(b))); w.toBytes() },
    "MultiArr5" to { b: ByteArray ->
        val w = JsonWriter(); MultiArr5Codec.encode(w, MultiArr5Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo1" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo1Codec.encode(w, OptCombo1Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo2" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo2Codec.encode(w, OptCombo2Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo3" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo3Codec.encode(w, OptCombo3Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo4" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo4Codec.encode(w, OptCombo4Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo5" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo5Codec.encode(w, OptCombo5Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo6" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo6Codec.encode(w, OptCombo6Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo7" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo7Codec.encode(w, OptCombo7Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo8" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo8Codec.encode(w, OptCombo8Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo9" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo9Codec.encode(w, OptCombo9Codec.decode(JsonReader(b))); w.toBytes() },
    "OptCombo10" to { b: ByteArray ->
        val w = JsonWriter(); OptCombo10Codec.encode(w, OptCombo10Codec.decode(JsonReader(b))); w.toBytes() },
    "NestInner" to { b: ByteArray ->
        val w = JsonWriter(); NestInnerCodec.encode(w, NestInnerCodec.decode(JsonReader(b))); w.toBytes() },
    "NestCoord" to { b: ByteArray ->
        val w = JsonWriter(); NestCoordCodec.encode(w, NestCoordCodec.decode(JsonReader(b))); w.toBytes() },
    "NestIdVal" to { b: ByteArray ->
        val w = JsonWriter(); NestIdValCodec.encode(w, NestIdValCodec.decode(JsonReader(b))); w.toBytes() },
    "NestLabel" to { b: ByteArray ->
        val w = JsonWriter(); NestLabelCodec.encode(w, NestLabelCodec.decode(JsonReader(b))); w.toBytes() },
    "NestMoney" to { b: ByteArray ->
        val w = JsonWriter(); NestMoneyCodec.encode(w, NestMoneyCodec.decode(JsonReader(b))); w.toBytes() },
    "NestRange32" to { b: ByteArray ->
        val w = JsonWriter(); NestRange32Codec.encode(w, NestRange32Codec.decode(JsonReader(b))); w.toBytes() },
    "NestAddr" to { b: ByteArray ->
        val w = JsonWriter(); NestAddrCodec.encode(w, NestAddrCodec.decode(JsonReader(b))); w.toBytes() },
    "NestPoint3" to { b: ByteArray ->
        val w = JsonWriter(); NestPoint3Codec.encode(w, NestPoint3Codec.decode(JsonReader(b))); w.toBytes() },
    "OptNestInner" to { b: ByteArray ->
        val w = JsonWriter(); OptNestInnerCodec.encode(w, OptNestInnerCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestCoord" to { b: ByteArray ->
        val w = JsonWriter(); OptNestCoordCodec.encode(w, OptNestCoordCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestIdVal" to { b: ByteArray ->
        val w = JsonWriter(); OptNestIdValCodec.encode(w, OptNestIdValCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestLabel" to { b: ByteArray ->
        val w = JsonWriter(); OptNestLabelCodec.encode(w, OptNestLabelCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestMoney" to { b: ByteArray ->
        val w = JsonWriter(); OptNestMoneyCodec.encode(w, OptNestMoneyCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestRange32" to { b: ByteArray ->
        val w = JsonWriter(); OptNestRange32Codec.encode(w, OptNestRange32Codec.decode(JsonReader(b))); w.toBytes() },
    "OptNestAddr" to { b: ByteArray ->
        val w = JsonWriter(); OptNestAddrCodec.encode(w, OptNestAddrCodec.decode(JsonReader(b))); w.toBytes() },
    "OptNestPoint3" to { b: ByteArray ->
        val w = JsonWriter(); OptNestPoint3Codec.encode(w, OptNestPoint3Codec.decode(JsonReader(b))); w.toBytes() },
    "ModelArr1" to { b: ByteArray ->
        val w = JsonWriter(); ModelArr1Codec.encode(w, ModelArr1Codec.decode(JsonReader(b))); w.toBytes() },
    "ModelArr2" to { b: ByteArray ->
        val w = JsonWriter(); ModelArr2Codec.encode(w, ModelArr2Codec.decode(JsonReader(b))); w.toBytes() },
    "ModelArr3" to { b: ByteArray ->
        val w = JsonWriter(); ModelArr3Codec.encode(w, ModelArr3Codec.decode(JsonReader(b))); w.toBytes() },
    "ModelArr4" to { b: ByteArray ->
        val w = JsonWriter(); ModelArr4Codec.encode(w, ModelArr4Codec.decode(JsonReader(b))); w.toBytes() },
    "ModelArr5" to { b: ByteArray ->
        val w = JsonWriter(); ModelArr5Codec.encode(w, ModelArr5Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix01" to { b: ByteArray ->
        val w = JsonWriter(); Mix01Codec.encode(w, Mix01Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix02" to { b: ByteArray ->
        val w = JsonWriter(); Mix02Codec.encode(w, Mix02Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix03" to { b: ByteArray ->
        val w = JsonWriter(); Mix03Codec.encode(w, Mix03Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix04" to { b: ByteArray ->
        val w = JsonWriter(); Mix04Codec.encode(w, Mix04Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix05" to { b: ByteArray ->
        val w = JsonWriter(); Mix05Codec.encode(w, Mix05Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix06" to { b: ByteArray ->
        val w = JsonWriter(); Mix06Codec.encode(w, Mix06Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix07" to { b: ByteArray ->
        val w = JsonWriter(); Mix07Codec.encode(w, Mix07Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix08" to { b: ByteArray ->
        val w = JsonWriter(); Mix08Codec.encode(w, Mix08Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix09" to { b: ByteArray ->
        val w = JsonWriter(); Mix09Codec.encode(w, Mix09Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix10" to { b: ByteArray ->
        val w = JsonWriter(); Mix10Codec.encode(w, Mix10Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix11" to { b: ByteArray ->
        val w = JsonWriter(); Mix11Codec.encode(w, Mix11Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix12" to { b: ByteArray ->
        val w = JsonWriter(); Mix12Codec.encode(w, Mix12Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix13" to { b: ByteArray ->
        val w = JsonWriter(); Mix13Codec.encode(w, Mix13Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix14" to { b: ByteArray ->
        val w = JsonWriter(); Mix14Codec.encode(w, Mix14Codec.decode(JsonReader(b))); w.toBytes() },
    "Mix15" to { b: ByteArray ->
        val w = JsonWriter(); Mix15Codec.encode(w, Mix15Codec.decode(JsonReader(b))); w.toBytes() },
    "AllOpt1" to { b: ByteArray ->
        val w = JsonWriter(); AllOpt1Codec.encode(w, AllOpt1Codec.decode(JsonReader(b))); w.toBytes() },
    "AllOpt2" to { b: ByteArray ->
        val w = JsonWriter(); AllOpt2Codec.encode(w, AllOpt2Codec.decode(JsonReader(b))); w.toBytes() },
    "AllOpt3" to { b: ByteArray ->
        val w = JsonWriter(); AllOpt3Codec.encode(w, AllOpt3Codec.decode(JsonReader(b))); w.toBytes() },
    "AllOpt4" to { b: ByteArray ->
        val w = JsonWriter(); AllOpt4Codec.encode(w, AllOpt4Codec.decode(JsonReader(b))); w.toBytes() },
    "AllOpt5" to { b: ByteArray ->
        val w = JsonWriter(); AllOpt5Codec.encode(w, AllOpt5Codec.decode(JsonReader(b))); w.toBytes() },
    "RecList" to { b: ByteArray ->
        val w = JsonWriter(); RecListCodec.encode(w, RecListCodec.decode(JsonReader(b))); w.toBytes() },
    "RecTree" to { b: ByteArray ->
        val w = JsonWriter(); RecTreeCodec.encode(w, RecTreeCodec.decode(JsonReader(b))); w.toBytes() },
    "RecChain" to { b: ByteArray ->
        val w = JsonWriter(); RecChainCodec.encode(w, RecChainCodec.decode(JsonReader(b))); w.toBytes() },
    "RecWrap" to { b: ByteArray ->
        val w = JsonWriter(); RecWrapCodec.encode(w, RecWrapCodec.decode(JsonReader(b))); w.toBytes() },
    "RecWide" to { b: ByteArray ->
        val w = JsonWriter(); RecWideCodec.encode(w, RecWideCodec.decode(JsonReader(b))); w.toBytes() },
    "Wide20" to { b: ByteArray ->
        val w = JsonWriter(); Wide20Codec.encode(w, Wide20Codec.decode(JsonReader(b))); w.toBytes() },
    "Wide25" to { b: ByteArray ->
        val w = JsonWriter(); Wide25Codec.encode(w, Wide25Codec.decode(JsonReader(b))); w.toBytes() },
    "Wide30" to { b: ByteArray ->
        val w = JsonWriter(); Wide30Codec.encode(w, Wide30Codec.decode(JsonReader(b))); w.toBytes() },
    "Wide35" to { b: ByteArray ->
        val w = JsonWriter(); Wide35Codec.encode(w, Wide35Codec.decode(JsonReader(b))); w.toBytes() },
    "Wide40" to { b: ByteArray ->
        val w = JsonWriter(); Wide40Codec.encode(w, Wide40Codec.decode(JsonReader(b))); w.toBytes() },
    "EdgeEmpty" to { b: ByteArray ->
        val w = JsonWriter(); EdgeEmptyCodec.encode(w, EdgeEmptyCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeOneOpt" to { b: ByteArray ->
        val w = JsonWriter(); EdgeOneOptCodec.encode(w, EdgeOneOptCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeBigNums" to { b: ByteArray ->
        val w = JsonWriter(); EdgeBigNumsCodec.encode(w, EdgeBigNumsCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeZeroVals" to { b: ByteArray ->
        val w = JsonWriter(); EdgeZeroValsCodec.encode(w, EdgeZeroValsCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeNullable" to { b: ByteArray ->
        val w = JsonWriter(); EdgeNullableCodec.encode(w, EdgeNullableCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeNegZero" to { b: ByteArray ->
        val w = JsonWriter(); EdgeNegZeroCodec.encode(w, EdgeNegZeroCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeNullByte" to { b: ByteArray ->
        val w = JsonWriter(); EdgeNullByteCodec.encode(w, EdgeNullByteCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeBoundary" to { b: ByteArray ->
        val w = JsonWriter(); EdgeBoundaryCodec.encode(w, EdgeBoundaryCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeStrLen" to { b: ByteArray ->
        val w = JsonWriter(); EdgeStrLenCodec.encode(w, EdgeStrLenCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeBytesLen" to { b: ByteArray ->
        val w = JsonWriter(); EdgeBytesLenCodec.encode(w, EdgeBytesLenCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeArrEmpty" to { b: ByteArray ->
        val w = JsonWriter(); EdgeArrEmptyCodec.encode(w, EdgeArrEmptyCodec.decode(JsonReader(b))); w.toBytes() },
    "EdgeArrBoundary" to { b: ByteArray ->
        val w = JsonWriter(); EdgeArrBoundaryCodec.encode(w, EdgeArrBoundaryCodec.decode(JsonReader(b))); w.toBytes() },
    "OptArr1" to { b: ByteArray ->
        val w = JsonWriter(); OptArr1Codec.encode(w, OptArr1Codec.decode(JsonReader(b))); w.toBytes() },
    "OptArr2" to { b: ByteArray ->
        val w = JsonWriter(); OptArr2Codec.encode(w, OptArr2Codec.decode(JsonReader(b))); w.toBytes() },
    "OptArr4" to { b: ByteArray ->
        val w = JsonWriter(); OptArr4Codec.encode(w, OptArr4Codec.decode(JsonReader(b))); w.toBytes() },
    "OptArr5" to { b: ByteArray ->
        val w = JsonWriter(); OptArr5Codec.encode(w, OptArr5Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOpt1" to { b: ByteArray ->
        val w = JsonWriter(); NestOpt1Codec.encode(w, NestOpt1Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOpt2" to { b: ByteArray ->
        val w = JsonWriter(); NestOpt2Codec.encode(w, NestOpt2Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOpt3" to { b: ByteArray ->
        val w = JsonWriter(); NestOpt3Codec.encode(w, NestOpt3Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOpt4" to { b: ByteArray ->
        val w = JsonWriter(); NestOpt4Codec.encode(w, NestOpt4Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOpt5" to { b: ByteArray ->
        val w = JsonWriter(); NestOpt5Codec.encode(w, NestOpt5Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOptInner1" to { b: ByteArray ->
        val w = JsonWriter(); NestOptInner1Codec.encode(w, NestOptInner1Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOptInner2" to { b: ByteArray ->
        val w = JsonWriter(); NestOptInner2Codec.encode(w, NestOptInner2Codec.decode(JsonReader(b))); w.toBytes() },
    "NestOptInner3" to { b: ByteArray ->
        val w = JsonWriter(); NestOptInner3Codec.encode(w, NestOptInner3Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest1" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest1Codec.encode(w, DeepNest1Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest2" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest2Codec.encode(w, DeepNest2Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest3" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest3Codec.encode(w, DeepNest3Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest4" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest4Codec.encode(w, DeepNest4Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest5" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest5Codec.encode(w, DeepNest5Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest6" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest6Codec.encode(w, DeepNest6Codec.decode(JsonReader(b))); w.toBytes() },
    "DeepNest7" to { b: ByteArray ->
        val w = JsonWriter(); DeepNest7Codec.encode(w, DeepNest7Codec.decode(JsonReader(b))); w.toBytes() },
    "TimestampEntry" to { b: ByteArray ->
        val w = JsonWriter(); TimestampEntryCodec.encode(w, TimestampEntryCodec.decode(JsonReader(b))); w.toBytes() },
    "ConfigEntry" to { b: ByteArray ->
        val w = JsonWriter(); ConfigEntryCodec.encode(w, ConfigEntryCodec.decode(JsonReader(b))); w.toBytes() },
)

val dumpRoundtrip: Map<String, (ByteArray, Boolean) -> String> = mapOf(
    "Inner" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpInner(InnerCodec.decode(r)) },
    "Coord" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpCoord(CoordCodec.decode(r)) },
    "IdVal" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpIdVal(IdValCodec.decode(r)) },
    "Label" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpLabel(LabelCodec.decode(r)) },
    "Money" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMoney(MoneyCodec.decode(r)) },
    "Range32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRange32(Range32Codec.decode(r)) },
    "Addr" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAddr(AddrCodec.decode(r)) },
    "Point3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPoint3(Point3Codec.decode(r)) },
    "SingleString" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleString(SingleStringCodec.decode(r)) },
    "SingleBoolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleBoolean(SingleBooleanCodec.decode(r)) },
    "SingleInt8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleInt8(SingleInt8Codec.decode(r)) },
    "SingleInt16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleInt16(SingleInt16Codec.decode(r)) },
    "SingleInt32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleInt32(SingleInt32Codec.decode(r)) },
    "SingleInt64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleInt64(SingleInt64Codec.decode(r)) },
    "SingleUint8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleUint8(SingleUint8Codec.decode(r)) },
    "SingleUint16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleUint16(SingleUint16Codec.decode(r)) },
    "SingleUint32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleUint32(SingleUint32Codec.decode(r)) },
    "SingleUint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleUint64(SingleUint64Codec.decode(r)) },
    "SingleFloat32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleFloat32(SingleFloat32Codec.decode(r)) },
    "SingleFloat64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleFloat64(SingleFloat64Codec.decode(r)) },
    "SingleBytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpSingleBytes(SingleBytesCodec.decode(r)) },
    "PairString" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairString(PairStringCodec.decode(r)) },
    "PairBoolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairBoolean(PairBooleanCodec.decode(r)) },
    "PairInt8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairInt8(PairInt8Codec.decode(r)) },
    "PairInt16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairInt16(PairInt16Codec.decode(r)) },
    "PairInt32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairInt32(PairInt32Codec.decode(r)) },
    "PairInt64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairInt64(PairInt64Codec.decode(r)) },
    "PairUint8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairUint8(PairUint8Codec.decode(r)) },
    "PairUint16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairUint16(PairUint16Codec.decode(r)) },
    "PairUint32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairUint32(PairUint32Codec.decode(r)) },
    "PairUint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairUint64(PairUint64Codec.decode(r)) },
    "PairFloat32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairFloat32(PairFloat32Codec.decode(r)) },
    "PairFloat64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairFloat64(PairFloat64Codec.decode(r)) },
    "PairBytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpPairBytes(PairBytesCodec.decode(r)) },
    "DualStringInt32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringInt32(DualStringInt32Codec.decode(r)) },
    "DualStringBoolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringBoolean(DualStringBooleanCodec.decode(r)) },
    "DualStringFloat64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringFloat64(DualStringFloat64Codec.decode(r)) },
    "DualStringBytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringBytes(DualStringBytesCodec.decode(r)) },
    "DualInt32Boolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt32Boolean(DualInt32BooleanCodec.decode(r)) },
    "DualInt32Float64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt32Float64(DualInt32Float64Codec.decode(r)) },
    "DualInt32Int64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt32Int64(DualInt32Int64Codec.decode(r)) },
    "DualInt32Uint32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt32Uint32(DualInt32Uint32Codec.decode(r)) },
    "DualInt64Uint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt64Uint64(DualInt64Uint64Codec.decode(r)) },
    "DualFloat32Float64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualFloat32Float64(DualFloat32Float64Codec.decode(r)) },
    "DualFloat64Boolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualFloat64Boolean(DualFloat64BooleanCodec.decode(r)) },
    "DualFloat64Bytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualFloat64Bytes(DualFloat64BytesCodec.decode(r)) },
    "DualUint32Uint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualUint32Uint64(DualUint32Uint64Codec.decode(r)) },
    "DualBooleanBytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualBooleanBytes(DualBooleanBytesCodec.decode(r)) },
    "DualInt8Uint8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt8Uint8(DualInt8Uint8Codec.decode(r)) },
    "DualInt16Uint16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt16Uint16(DualInt16Uint16Codec.decode(r)) },
    "DualStringInt64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringInt64(DualStringInt64Codec.decode(r)) },
    "DualStringUint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualStringUint64(DualStringUint64Codec.decode(r)) },
    "DualInt32Bytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt32Bytes(DualInt32BytesCodec.decode(r)) },
    "DualFloat64Int32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualFloat64Int32(DualFloat64Int32Codec.decode(r)) },
    "DualBooleanInt32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualBooleanInt32(DualBooleanInt32Codec.decode(r)) },
    "DualBytesInt64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualBytesInt64(DualBytesInt64Codec.decode(r)) },
    "DualInt8Float32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt8Float32(DualInt8Float32Codec.decode(r)) },
    "DualUint8Int16" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualUint8Int16(DualUint8Int16Codec.decode(r)) },
    "DualInt64Float64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualInt64Float64(DualInt64Float64Codec.decode(r)) },
    "DualUint64String" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDualUint64String(DualUint64StringCodec.decode(r)) },
    "Triple01" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple01(Triple01Codec.decode(r)) },
    "Triple02" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple02(Triple02Codec.decode(r)) },
    "Triple03" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple03(Triple03Codec.decode(r)) },
    "Triple04" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple04(Triple04Codec.decode(r)) },
    "Triple05" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple05(Triple05Codec.decode(r)) },
    "Triple06" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple06(Triple06Codec.decode(r)) },
    "Triple07" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple07(Triple07Codec.decode(r)) },
    "Triple08" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple08(Triple08Codec.decode(r)) },
    "Triple09" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple09(Triple09Codec.decode(r)) },
    "Triple10" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple10(Triple10Codec.decode(r)) },
    "Triple11" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple11(Triple11Codec.decode(r)) },
    "Triple12" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple12(Triple12Codec.decode(r)) },
    "Triple13" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple13(Triple13Codec.decode(r)) },
    "Triple14" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple14(Triple14Codec.decode(r)) },
    "Triple15" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTriple15(Triple15Codec.decode(r)) },
    "Five01" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive01(Five01Codec.decode(r)) },
    "Five02" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive02(Five02Codec.decode(r)) },
    "Five03" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive03(Five03Codec.decode(r)) },
    "Five04" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive04(Five04Codec.decode(r)) },
    "Five05" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive05(Five05Codec.decode(r)) },
    "Five06" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive06(Five06Codec.decode(r)) },
    "Five07" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive07(Five07Codec.decode(r)) },
    "Five08" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive08(Five08Codec.decode(r)) },
    "Five09" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive09(Five09Codec.decode(r)) },
    "Five10" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpFive10(Five10Codec.decode(r)) },
    "Ten01" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTen01(Ten01Codec.decode(r)) },
    "Ten02" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTen02(Ten02Codec.decode(r)) },
    "Ten03" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTen03(Ten03Codec.decode(r)) },
    "Ten04" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTen04(Ten04Codec.decode(r)) },
    "Ten05" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTen05(Ten05Codec.decode(r)) },
    "ArrString" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrString(ArrStringCodec.decode(r)) },
    "ArrInt32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrInt32(ArrInt32Codec.decode(r)) },
    "ArrBoolean" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrBoolean(ArrBooleanCodec.decode(r)) },
    "ArrFloat64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrFloat64(ArrFloat64Codec.decode(r)) },
    "ArrBytes" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrBytes(ArrBytesCodec.decode(r)) },
    "ArrInt64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrInt64(ArrInt64Codec.decode(r)) },
    "ArrUint64" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpArrUint64(ArrUint64Codec.decode(r)) },
    "MultiArr1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMultiArr1(MultiArr1Codec.decode(r)) },
    "MultiArr2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMultiArr2(MultiArr2Codec.decode(r)) },
    "MultiArr3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMultiArr3(MultiArr3Codec.decode(r)) },
    "MultiArr4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMultiArr4(MultiArr4Codec.decode(r)) },
    "MultiArr5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMultiArr5(MultiArr5Codec.decode(r)) },
    "OptCombo1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo1(OptCombo1Codec.decode(r)) },
    "OptCombo2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo2(OptCombo2Codec.decode(r)) },
    "OptCombo3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo3(OptCombo3Codec.decode(r)) },
    "OptCombo4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo4(OptCombo4Codec.decode(r)) },
    "OptCombo5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo5(OptCombo5Codec.decode(r)) },
    "OptCombo6" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo6(OptCombo6Codec.decode(r)) },
    "OptCombo7" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo7(OptCombo7Codec.decode(r)) },
    "OptCombo8" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo8(OptCombo8Codec.decode(r)) },
    "OptCombo9" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo9(OptCombo9Codec.decode(r)) },
    "OptCombo10" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptCombo10(OptCombo10Codec.decode(r)) },
    "NestInner" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestInner(NestInnerCodec.decode(r)) },
    "NestCoord" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestCoord(NestCoordCodec.decode(r)) },
    "NestIdVal" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestIdVal(NestIdValCodec.decode(r)) },
    "NestLabel" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestLabel(NestLabelCodec.decode(r)) },
    "NestMoney" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestMoney(NestMoneyCodec.decode(r)) },
    "NestRange32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestRange32(NestRange32Codec.decode(r)) },
    "NestAddr" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestAddr(NestAddrCodec.decode(r)) },
    "NestPoint3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestPoint3(NestPoint3Codec.decode(r)) },
    "OptNestInner" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestInner(OptNestInnerCodec.decode(r)) },
    "OptNestCoord" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestCoord(OptNestCoordCodec.decode(r)) },
    "OptNestIdVal" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestIdVal(OptNestIdValCodec.decode(r)) },
    "OptNestLabel" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestLabel(OptNestLabelCodec.decode(r)) },
    "OptNestMoney" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestMoney(OptNestMoneyCodec.decode(r)) },
    "OptNestRange32" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestRange32(OptNestRange32Codec.decode(r)) },
    "OptNestAddr" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestAddr(OptNestAddrCodec.decode(r)) },
    "OptNestPoint3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptNestPoint3(OptNestPoint3Codec.decode(r)) },
    "ModelArr1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpModelArr1(ModelArr1Codec.decode(r)) },
    "ModelArr2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpModelArr2(ModelArr2Codec.decode(r)) },
    "ModelArr3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpModelArr3(ModelArr3Codec.decode(r)) },
    "ModelArr4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpModelArr4(ModelArr4Codec.decode(r)) },
    "ModelArr5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpModelArr5(ModelArr5Codec.decode(r)) },
    "Mix01" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix01(Mix01Codec.decode(r)) },
    "Mix02" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix02(Mix02Codec.decode(r)) },
    "Mix03" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix03(Mix03Codec.decode(r)) },
    "Mix04" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix04(Mix04Codec.decode(r)) },
    "Mix05" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix05(Mix05Codec.decode(r)) },
    "Mix06" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix06(Mix06Codec.decode(r)) },
    "Mix07" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix07(Mix07Codec.decode(r)) },
    "Mix08" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix08(Mix08Codec.decode(r)) },
    "Mix09" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix09(Mix09Codec.decode(r)) },
    "Mix10" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix10(Mix10Codec.decode(r)) },
    "Mix11" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix11(Mix11Codec.decode(r)) },
    "Mix12" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix12(Mix12Codec.decode(r)) },
    "Mix13" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix13(Mix13Codec.decode(r)) },
    "Mix14" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix14(Mix14Codec.decode(r)) },
    "Mix15" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpMix15(Mix15Codec.decode(r)) },
    "AllOpt1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAllOpt1(AllOpt1Codec.decode(r)) },
    "AllOpt2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAllOpt2(AllOpt2Codec.decode(r)) },
    "AllOpt3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAllOpt3(AllOpt3Codec.decode(r)) },
    "AllOpt4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAllOpt4(AllOpt4Codec.decode(r)) },
    "AllOpt5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpAllOpt5(AllOpt5Codec.decode(r)) },
    "RecList" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRecList(RecListCodec.decode(r)) },
    "RecTree" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRecTree(RecTreeCodec.decode(r)) },
    "RecChain" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRecChain(RecChainCodec.decode(r)) },
    "RecWrap" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRecWrap(RecWrapCodec.decode(r)) },
    "RecWide" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpRecWide(RecWideCodec.decode(r)) },
    "Wide20" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpWide20(Wide20Codec.decode(r)) },
    "Wide25" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpWide25(Wide25Codec.decode(r)) },
    "Wide30" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpWide30(Wide30Codec.decode(r)) },
    "Wide35" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpWide35(Wide35Codec.decode(r)) },
    "Wide40" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpWide40(Wide40Codec.decode(r)) },
    "EdgeEmpty" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeEmpty(EdgeEmptyCodec.decode(r)) },
    "EdgeOneOpt" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeOneOpt(EdgeOneOptCodec.decode(r)) },
    "EdgeBigNums" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeBigNums(EdgeBigNumsCodec.decode(r)) },
    "EdgeZeroVals" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeZeroVals(EdgeZeroValsCodec.decode(r)) },
    "EdgeNullable" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeNullable(EdgeNullableCodec.decode(r)) },
    "EdgeNegZero" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeNegZero(EdgeNegZeroCodec.decode(r)) },
    "EdgeNullByte" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeNullByte(EdgeNullByteCodec.decode(r)) },
    "EdgeBoundary" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeBoundary(EdgeBoundaryCodec.decode(r)) },
    "EdgeStrLen" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeStrLen(EdgeStrLenCodec.decode(r)) },
    "EdgeBytesLen" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeBytesLen(EdgeBytesLenCodec.decode(r)) },
    "EdgeArrEmpty" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeArrEmpty(EdgeArrEmptyCodec.decode(r)) },
    "EdgeArrBoundary" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpEdgeArrBoundary(EdgeArrBoundaryCodec.decode(r)) },
    "OptArr1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptArr1(OptArr1Codec.decode(r)) },
    "OptArr2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptArr2(OptArr2Codec.decode(r)) },
    "OptArr4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptArr4(OptArr4Codec.decode(r)) },
    "OptArr5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpOptArr5(OptArr5Codec.decode(r)) },
    "NestOpt1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOpt1(NestOpt1Codec.decode(r)) },
    "NestOpt2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOpt2(NestOpt2Codec.decode(r)) },
    "NestOpt3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOpt3(NestOpt3Codec.decode(r)) },
    "NestOpt4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOpt4(NestOpt4Codec.decode(r)) },
    "NestOpt5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOpt5(NestOpt5Codec.decode(r)) },
    "NestOptInner1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOptInner1(NestOptInner1Codec.decode(r)) },
    "NestOptInner2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOptInner2(NestOptInner2Codec.decode(r)) },
    "NestOptInner3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpNestOptInner3(NestOptInner3Codec.decode(r)) },
    "DeepNest1" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest1(DeepNest1Codec.decode(r)) },
    "DeepNest2" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest2(DeepNest2Codec.decode(r)) },
    "DeepNest3" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest3(DeepNest3Codec.decode(r)) },
    "DeepNest4" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest4(DeepNest4Codec.decode(r)) },
    "DeepNest5" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest5(DeepNest5Codec.decode(r)) },
    "DeepNest6" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest6(DeepNest6Codec.decode(r)) },
    "DeepNest7" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpDeepNest7(DeepNest7Codec.decode(r)) },
    "TimestampEntry" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpTimestampEntry(TimestampEntryCodec.decode(r)) },
    "ConfigEntry" to { b: ByteArray, isMp: Boolean ->
        val r = if (isMp) MsgPackReader(b) else JsonReader(b)
        dumpConfigEntry(ConfigEntryCodec.decode(r)) },
)

fun runEmit(vecDir: String, outDir: String) {
    File(outDir).mkdirs()
    var fail = 0

    for (name in testModels) {
        try {
            val b = File(vecDir, "$name.msgpack").readBytes()
            File(outDir, "$name.msgpack").writeBytes(mpRoundtrip[name]!!(b))
            File(outDir, "$name.decoded.json").writeText(dumpRoundtrip[name]!!(b, true))
        } catch (e: Exception) { println("FAIL $name mp: $e"); fail++ }
        try {
            val b = File(vecDir, "$name.json").readBytes()
            File(outDir, "$name.json").writeBytes(jsonRoundtrip[name]!!(b))
            File(outDir, "$name.decoded.json").writeText(dumpRoundtrip[name]!!(b, false))
        } catch (e: Exception) { println("FAIL $name json: $e"); fail++ }
    }

    val total = testModels.size * 2
    println("emit-kotlin: ${total - fail} passed, $fail failed")
    if (fail > 0) System.exit(1)
}
